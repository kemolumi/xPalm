use std::time::Duration;

pub const POLLING_RATE: Duration = Duration::from_millis(8);
pub const CONNECT_COOLDOWN: Duration = Duration::from_secs(10);
pub const CODE_PERIOD: Duration = Duration::from_secs(30);
